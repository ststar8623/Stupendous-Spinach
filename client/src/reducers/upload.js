const INIT_STATE = {
  image: {
    url: null,
    latitude: 0,
    longitude: 0
  }
};

export default function images(state = INIT_STATE, action) {
  switch (action.type) {
  case 'IMAGE_UPLOAD':

  }
}